{
  "name": "tinooo",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client"
  },
  "dependencies": {
    "react-responsive": "^4.0.4"
  }
}
